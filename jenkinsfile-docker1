pipeline{
    agent{
        label 'docker-agent'
    }
    stages{
        stage('Provera'){
            steps{
            sh '''
            whoami
            echo "Docker Agent"
            cat /etc/os-release
            '''
        }
        }
    }
}